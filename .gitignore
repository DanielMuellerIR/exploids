.DS_Store
.build/
Packages/
# Generiertes App-Bundle (per build-app.sh erzeugt, nicht versionieren)
Exploids.app/
# Release-Artefakte (DMG + Notes) aus wrappers/sign-and-release.sh
build/
*.dmg
# Generierter DMG-Hintergrund (aus assets/generate-dmg-background.swift)
assets/dmg-background.png
# Lokale, private Agent-/Tool-Konfiguration (nicht öffentlich)
.agents/
.claude/
# Lokale Signing-/Notary-Konfiguration (Team-ID, Identity, Keychain-Profil) –
# maschinen-/account-spezifisch, gehört nicht ins öffentliche Repo
wrappers/release.local.sh
# iOS-XcodeGen-Artefakte (regenerierbar aus ios/project.yml)
ios/*.xcodeproj
ios/.build/
xcuserdata/
DerivedData/
